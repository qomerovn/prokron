# Greenfield continuity example

This realistic fixture follows a document-approval feature from thesis through
superseded decisions, dependent and parallel tasks, a model handoff, resumed
intent, and a checkpoint.

From this directory, run:

```bash
prokron doctor
prokron status
prokron next
prokron context T-002
```
